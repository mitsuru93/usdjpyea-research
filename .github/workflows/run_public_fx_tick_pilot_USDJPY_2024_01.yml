name: Run Public FX Tick Pilot 2024-01 USDJPY

on:
  workflow_dispatch:

jobs:
  usdjpy-tick-pilot:
    uses: ./.github/workflows/reusable_public_fx_tick_symbol_pilot_2024_01.yml
    with:
      symbol: USDJPY
      pilot_tag: pilot-2024-01-USDJPY
      min_coverage: "0.995"
      max_hard_errors: "20"

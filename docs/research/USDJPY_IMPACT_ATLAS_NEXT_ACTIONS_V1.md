# USDJPY Impact Atlas next actions v1

1. Merge the Phase 0 program package after CI.
2. Execute the Phase 0 workflow on exact main and retain the artifact identity.
3. Extend source inventory from repository files to accepted Releases, receipts, and hashes.
4. Freeze the Phase 1 canonical observation schema.
5. Implement loss-contribution and portfolio-cluster diagnostics.
6. Produce the first program-priority ranking without candidate selection.

A new conversation must begin from `docs/handoffs/USDJPY_IMPACT_ATLAS_HANDOFF_V1.md` and the latest GitHub main, not from chat summaries alone.

# USDJPY M15 Impulse Breakout H2 Pre-registration v1

## Status

```text
SUPERSEDED BEFORE H2 EXECUTION
```

This single-candidate pre-registration was committed before any 2024-07 through 2024-12 candidate result was inspected, but it is no longer the active test plan.

The project subsequently adopted a multi-family development and joint-validation design:

```text
2024-01 through 2024-06
  -> develop independent strategy families
  -> retain at most three representatives per family
  -> pre-register all retained candidates together
  -> test all candidates on 2024-07 through 2024-12 in one batch
```

Active plan:

```text
docs/research_reboot/usdjpy_multi_family_h1_research_plan_v1.md
```

Candidate registry:

```text
configs/research/usdjpy_h1_multi_family_candidates_v1.json
```

The original impulse candidate remains unchanged and is included as candidate `A1_impulse_breakout_lb3_hold6` in the multi-family registry.

No H2 candidate results were accepted under this superseded document. Its previous content remains recoverable from Git history at commit:

```text
fc35f780659fb97e3bec5a32e74276baa868da0b
```

A new joint H2 pre-registration will be created only after the H1 multi-family workflow has been executed and the retained candidate list has been fixed.
